module.exports = {
    "name":"apply",
    "description": "For manually changing all user's nickname with channel prefix",
    execute(message, args, db){

        // sql: 取得該伺服器的所有原名資料
        // 處理: 承上，將原名資料改為Map結構，key為user_id, value 為 originame

        // discord api : 把所有有在語音群的人抓出來

        // discord api : 承上的discord資料，跑迴圈，若有在sql提供的map裡，改名

        // 將改名function回傳的promises放在陣列裡
        // 回傳Promise.all.then()

        // 收尾訊息

        // catch

    }

}